"""Admin API — policy management, configuration, and system controls."""

from fastapi import APIRouter

router = APIRouter()

# TODO: implement admin endpoints (CRUD for policies, settings management)
